#include <stdio.h>
#include "main.h"

/**
 * main - Test if given value is positive or negative
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a = 0;

	positive_or_negative(a);
	
	return (0);
}
